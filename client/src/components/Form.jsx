export default function Form() {
  // we need state to save the formValues
  // we do not need useEffect here
  // a submit handler
  function handleSubmit() {
    // prevent default behaviour
    // something to fetch post endpoint in th server
    //  fetch("url", {})
    // we need to handle the change
  }

  return (
    <>
      <form>
        {/* we need to track the change in the inputs of our form  */}
        {/* we need to submit event to send the data  */}
      </form>
    </>
  );
}
